---
name: blackbox
description: Sets up and maintains a rigorous, event-sourcing-flavored project record (STATUS snapshot, append-only CHANGELOG, ADR decision log, plus graduated files for fault cases / decision history / multi-copy sync / operating procedures) so that any project — code repo, physical/infrastructure deployment, or long-running system — can be picked back up safely after any gap, even years, by someone with no prior context. Replaces the earlier "logbook" skill; there is no lightweight vs. detailed mode — detail level emerges naturally from how often the fixed trigger rules actually fire in a given project, not from a chosen setting. ONLY use this skill when the user EXPLICITLY asks to enable/apply it for a project — e.g. "启用 blackbox", "用 blackbox 记录这个项目", "这个项目按 blackbox 来", "blackbox this project". Do NOT proactively apply this to any project or long-running task just because the work is multi-step or spans many turns — explicit invocation is required every time, no exceptions.
---

# blackbox

一套"事件溯源风格"的项目记录框架：不是真的做事件溯源系统，是借用它的几条纪律
（只追加不改写、任何推翻都新增记录而非抹掉旧账、任意历史时刻的状态要能还原）
来指导人和 AI 该怎么记录一个项目，使得多年后任何不了解背景的人，都能安全地
接手继续工作。

## 记录的写入时机（write-through，不是收尾动作）

**触发规则命中的那一刻，记录必须立即写入文件，不能记在"待会儿一起写"的
待办里、更不能攒到操作全部完成后再一次性补写。** 这条对 AI agent 尤其
重要，原因直接关系到 blackbox 的核心目的：

> blackbox 存在的意义之一，就是哪怕会话中途中断（agent 崩溃、断线、
> token 用尽、被切换任务），已经发生的部分也已经落盘、不会凭空消失。
> 如果记录被当成"操作做完之后的收尾工作"，一旦在收尾之前中断，这部分
> 工作就真的没有留下任何痕迹——这跟"从没做过"没有区别，直接违背了
> blackbox 要解决的问题。

**核心心智模型：不是"做完事之后要记得记录"，是"记录没有落盘之前，这件
事就不算完成"。** 把记录当成完成判定本身的一部分，而不是完成之后追加的
义务——这个框架下，"忘了记"和"没做完"是同一件事，agent 不会把它当成
可以延后的次要任务。

**记录的颗粒度，直接复用规则 1 已经定义的"原子单位"（意图变了一次/策略
变了一次/产生了可观察的不同结果），不需要另一套标准。** 这同时解决两个
方向的问题：既不会把一次连贯操作拆得过碎（改一个变量名不用单独记一条，
只要还是同一个意图），也不会把好几个独立意图打包成一条记了事。

**骨架先行（谨慎/多步骤的原子操作，开始前先写骨架）**：对于会跨越多个
工具调用、或者本身有一定风险的原子操作，**在开始执行之前**，先写一条
最简单的骨架记录（时间戳 + 打算做什么 + 状态：进行中），操作完成后，
**在同一条记录上补全**结果和验证字段，不新开一条。这样即使操作执行到
一半会话中断，至少留下"有个事情开始了但没做完"的痕迹，而不是现在这样
"中断了就跟没发生过一样"。日常的小改动（比如加一行配置）不需要这个
两阶段流程，直接完成后立即写一条完整记录即可——骨架先行只在"操作本身
会跨越明显的时间/步骤、有中断风险"时才用。

**写入失败时的处理**：如果记录本身写不进去（文件被锁、磁盘满、权限
问题等），不能忽略掉、假装没发生继续往下做——必须停下后续操作，直到
写入问题解决，或者至少把这个失败本身标记出来，不能让"记录失败"这件事
本身也悄悄消失。

具体要求：
- 改一次代码 → 改完立刻追加一条 CHANGELOG 记录，再继续下一步，不是把
  "改代码"和"记录改了什么"分成两个可以先后错开的任务
- 验证一次结果 → 验证完立刻补上这次记录的验证字段，不是等全部工作做完
  再回头统一补验证结果
- STATUS 的关键事实区/项目状态/已知盲点表发生变更 → 变更后立刻在
  CHANGELOG 留一条对应记录（这条在"核心文件"一节已经提过，这里重申
  它同样适用"立即写"，不是"记得写"）
- **不要等用户提醒（比如问一句"记录了吧？"）才回头补记**——用户的提醒
  是兜底，不是记录节奏的正常组成部分。如果只有在被追问之后才补记，
  说明记录已经变成了收尾动作而不是操作本身的一部分，这正是本节要
  避免的失效模式

**如果执行环境支持工具层封装**（比如把"改文件"和"写记录"包装成同一个
工具调用，或者有能拦截"未记录变更"的校验机制），效果会比单纯依赖文字
规则更可靠——blackbox 本身只是一份文字规范，管不到执行环境这一层，但
如果你有能力加这层机制，建议加上，不要只依赖 agent 自觉遵守文字规则。

## 修改这份 SKILL.md 本身，需要走确认流程

blackbox 是跨项目共享的规则定义，不是任何单个项目的专属文件。**任何
agent（包括在具体项目里执行任务的 agent）如果认为 blackbox 的规则本身
有漏洞、需要修改，不能自己直接改这份 SKILL.md**——这类改动会影响到所有
以后用到这套规则的项目，按 blackbox 自己的精神，这属于"架构级、影响面大、
以后会被问为什么"的决定，应该先跟用户确认、由用户（或用户认可的正式流程）
更新官方版本，而不是每个 agent 各自维护一份改过的本地副本，导致版本分叉、
没人说得清哪个是准的。

**发现疑似漏洞时的正确路径**：不是随手在 CHANGELOG 里提一句（CHANGELOG
是给"已确认生效"的变更用的，混入"怀疑有问题"的内容会污染历史记录），
而是在当前项目的 STATUS"已知盲点表"或专门的疑点记录里，用结构化的方式
写清楚：发现的场景、具体表现、影响、建议的修改方向，标记"待人工确认"。
只有得到用户明确同意后，才能去改共享定义。

**如果执行环境支持**，把共享规范定义文件设成只读（比如文件系统权限），
比单纯写"不能改"这条文字规则更可靠——文字规则终究是软约束，技术上直接
不给写权限是硬约束。做不到的话，至少要让 agent 知道"没有权限"和"有权限
但被要求不用"是两种不同强度的约束，后者更容易被绕过。

**顺带一提**：修改共享规范这件事本身，也受上面"记录的写入时机"这条规则
约束——决定要提出这类疑点时，同样要在发现的当下就记录，不能自己先斟酌
好了、准备好修改方案了才一次性上报。

## 触发条件（重要，务必遵守）

**只有用户明确要求启用时才使用这个 Skill。** 例如用户说："启用 blackbox"、
"这个项目用 blackbox"、"blackbox this project"。

**即使当前工作明显是多轮、持续、实质性的，只要用户没有明确提及要用
blackbox，也不要主动套用这套结构或主动创建记录文件。** 可以在合适的时机
简短提一句"这类项目也可以用 blackbox 记录，需要的话说一声"，但不要未经
同意就动手创建文件。

## 核心原则（判断一切取舍的统一标准）

**不设"简单模式 vs 详细模式"。** 只有一组固定的、"事件一旦发生就必须记录"
的触发规则（见下）。记录的详细程度，是这些规则在具体项目里实际触发次数的
自然结果——项目越复杂、风险越高，触发次数越多，记录自然越详细；简单项目
触发次数少，记录自然精简。不需要，也不允许，为了"图省事"而判断"这次要不要
认真记"——只要事件对上了某条规则，就记，规则本身就是触发条件。

**判断"某条信息该不该被记录"的统一试金石**：
> 一个不知道任何背景、但知道去哪儿找的人，读完这套文件后，能不能达到
> "可以安全地继续动手"这个状态？
信息不够会不安全（重复踩坑、碰红线、不清楚现状就贸然改）；信息太多会导致
没人愿意维护、最终被放弃，本质上也是不安全。

**元规则（多条规则可以被同一条记录共同满足）**：一个事件经常会同时命中好几条
触发规则（比如一次不可逆的回滚操作，同时命中"多次尝试""决定被推翻""结果不
符预期""不可逆操作"）。**这不要求写好几条独立记录**——用一条记录说清楚，在
"类型"字段里把命中的几类都标上即可。不要把这套规则理解成"按规则打卡"。

## 16 条触发规则

事件一旦发生，无条件记录，不需要判断"这次要不要认真记"：

1. 一件事发生几次实际尝试/调整，就记几条，各自带时间戳。**"一次尝试"的
   最小颗粒度是"意图变了一次/策略变了一次/产生了可观察的不同结果"**，不是
   "每敲一次命令"——同一意图下的连续微调（比如试了 5 个参数值）可以合并成
   一条，把过程细节写在同一条记录里
2. 覆盖或删除有意义的现有文件/状态前，先留一份备份记录。物理/非文件状态
   场景，"备份"可以是照片、读数记录、接线拓扑图等"破坏性操作前的状态快照"，
   不强求真的能"恢复"，但要留痕。如果这一步因客观原因做不到（没法备份、
   没时间备份），按规则 16 处理——记录"明知没法备份，仍然执行"这个决定
3. 任何过去的决定被推翻时，新增记录说明推翻了什么，不删除或修改旧记录。
   **这条和规则 15 的边界**：规则 3 处理"当时是对的/是正式成立的决定，
   现在不再采用"（认知或情况变了）；规则 15 处理"记录表达的内容，从写下的
   那一刻起就跟当时真实情况不符"（记错了）。两者可能同时命中同一个事件，
   按元规则处理，不用纠结归到哪一条
4. 存在"多份可能漂移的副本"时，每次同步/核对状态都记一笔。日常同步事件
   记入 CHANGELOG（类型：同步）即可；当副本数量达到"毕业"阈值（见下）时，
   才需要独立的同步地图文件
5. 发现"本该记录但当时没记"的信息缺口，发现的当下就记，不用等补上了才记。
   落地位置是 CHANGELOG（当下发现的时刻快照）；STATUS 的"已知盲点表"只是
   "目前仍未解决的缺口"视图——缺口被补上后从 STATUS 删掉，CHANGELOG 的
   原始记录保留不动，两者不是重复记两遍，是同一事实的两种用途
6. 操作结果和预期不一致（无论好坏），都记。**渐变式偏差**（性能慢慢变慢、
   资源占用慢慢升高这种没有明确发生时刻的偏差）不要求每次观察到就记，而是
   **项目开始时约定一个可衡量的阈值，触及阈值时才触发**——否则会被日常的
   噪音式波动淹没
7. 依赖的外部事物（不受你控制的服务/供应商/政策/协议）发生变化时，记
8. 决策依赖的假设——被固化为后续判断的前提、被验证、或被证伪，都记
9. 明确排除/放弃的备选方案，连同排除理由一起记
10. 约束条件（预算、时间、硬件、法规等）发生变化时记
11. 有意识接受一个已知风险/技术债、决定先不处理，这个决定本身记
12. 项目目标/范围/验收标准本身发生变化时记
13. 依赖的运行环境/底层依赖发生漂移时记（操作系统、硬件、网络拓扑、工具链
    版本等——代码没动，但运行它的地基变了）
14. 权限/责任/所有权发生交接时记，**包括 AI 协作场景下的会话交接**——交接
    记录要包含：交接时已知的事实、未完成事项、当前活跃的假设/未决问题，
    不能只写一句"我接手了"
15. 记录本身被发现有误、或多份记录互相矛盾时，追加更正说明，**不覆盖旧
    记录**——这样"曾经记错过"这件事本身也留痕，而不是悄悄改成看起来像
    从未出错过
16. 执行不可逆操作前，记录"当时明知不可逆、仍然决定做"的意图——这条记的
    是决策意图，跟规则 2 的"备份"不是一回事：备份保护数据，这条保护"当时
    有没有把握、是不是赌了一把"这个判断依据

## 核心文件

**STATUS**（现状快照，唯一整体改写型文件）
- **关键事实区**：高频查询、易记错的硬数字（仓库地址、版本号、部署路径、
  最终产出链接等），YAML 风格，按项目实际情况增减字段
- **项目状态**：固定枚举——`进行中` / `已完成维护中` / `已归档` / `已废弃
  （原因: xxx，替代方案: xxx）`，不用自由文本
- **已知盲点表**：内容 + 状态（未补/已补，已补的注明日期）
- **叙事部分**：当前阶段 / 上次做了什么 / 下一步待办（带截止日期的清单，
  过期没勾的要写一句解释——延迟了还是不需要了）
- **软锁标记**（顶部，写之前留、写完清）：`🔒 占用中 | 谁 | 从: 时间戳 |
  打算做: 一句话 | 预计时长(TTL): 可选但建议填`
- **约定**：关键事实区/项目状态/已知盲点表的任何变更，必须在 CHANGELOG
  留一条对应记录（哪怕只有一行）；叙事部分的措辞调整可以豁免，不用每次
  都记
- 开工时核对（不准就顺手核正，这是维护记录准确性，不需要额外确认），
  收工前必须完整更新关键事实/项目状态/盲点表；叙事部分来不及也要先把
  盲点表更新对

**CHANGELOG**（时间线日志，只追加不改写）

字段分两层，避免"要求太多、最后全部沦为敷衍"：

*基础字段（任何一条都必须有）*：
- 开始时间戳、结束时间戳（格式见下）
- 类型（可多选，命中几类写几类）：新增 / 修改 / 移除 / 撤回 / 升级 / 修复 /
  决策 / 重建 / 同步 / 不可逆
- 涉及对象/文件（鼓励带版本指纹：commit hash / image digest / config
  hash，没有就写"未知"进已知盲点）
- 原因

*扩展字段（仅当类型包含"决策/修复/重建/撤回/不可逆"任一项时才强制，
其余类型可留空）*：
- 影响
- 验证结果：枚举——`通过` / `失败` / `部分` / `未验证` / `不适用`（允许
  显式写"未验证"，这比不写或者假装验证过更诚实、更有用）
- 是否后续被撤回：是/否（后续真被撤回时，不是回来改这条，是新增一条新
  记录，在新记录里关联回这条）
- 依赖的未验证假设（对应规则 8，可选）
- `corrected_by`：如果这条记录后来被规则 15 更正过，指向更正那条记录的
  编号/时间戳（默认空）

*建议字段（不强制，AI 协作场景下建议启用）*：
- `author`: human / ai
- `verified_by`: human / 空（AI 写的记录，人有没有复核过）

- 关联：ADR / 故障案例 / 其他 CHANGELOG 条目编号

*归档*：CHANGELOG 只追加不代表永远不整理——项目运行足够久之后（按年或
按重大里程碑），把旧记录移到 `archive/CHANGELOG-<年份>.md`，当前文件只
保留近期记录，STATUS 里留一条"历史日志归档索引"指向归档文件。这不是新的
触发规则，是长期维护项目的一条约定，具体节奏按项目实际膨胀速度定。

**ADR**（架构决策记录）

字段：背景、考虑过的选项、决定、理由、影响，加三个**永远存在**的字段
（没有就写"无"，不能省略）：
- **不确定性**：这个决定依赖了哪些还没验证的假设（对应规则 8）
- **撤销/回退方案**：如果这个决定错了，具体怎么退回去（对应规则 16 的
  决策意图精神）
- **接受的风险/未处理的问题**：明知有问题但决定先不处理的部分（对应规则 11）

状态用固定枚举：`proposed` / `accepted` / `superseded`（被取代，注明被哪个
ADR 取代）/ `deprecated`。被取代不删除旧文件。

**原则上 ADR 必须在决定成立的当下写，不能事后补**——但如果真的发现了一个
"当年该写但没写"的决定（对应规则 5），允许通过一个显式标注的例外通道补记：
状态字段加 `补记: 是`，正文注明"此决定实际发生于 X 时间，因 Y 原因于今日
补记，依据是 Z"。这不是"允许事后洗白"——洗白是悄悄改成看起来像当时写的，
补记是诚实承认"这是后来才想起来该记的"，两者性质不同。

## "毕业"文件（不预先都建，达到条件后从核心文件里拆分出来）

- **故障案例库**：同类排查事件（症状/原因/解决过程/教训结构，"同类"按
  根因/症状族/排查路径模板判断）第 3 次出现时拆分独立；**或者单次事件
  已经造成足够严重的影响**（比如导致人工介入超过一定时长、影响到外部
  用户、需要执行回滚）——满足任一条即可，不用两个都等到
- **决策变更索引表**：存在触发，第一次发生"决策被取代"时就建（`adr/README.md`，
  表格：编号/状态/被谁取代/日期）。ADR 状态变更时，索引表要跟着同一个
  动作一起更新，不能事后补
- **多副本同步地图**：当同一份东西存在 **2 份或以上**"仍在被读写/使用、
  且没有自动强同步机制"的副本时建立（纯只读备份、定期打包的归档快照不算
  在内）。专门记录"哪份是权威版本"
- **操作手册（SOP）**：只要项目里存在至少一个会被反复执行的操作（升级/
  重建/回滚/协作者接手），就该写，不用等真的重复执行过——哪怕第一版
  只有最简单的步骤+回滚方式，也好过没有。与归档文档性质不同：归档文档
  回答"发生过什么"，SOP 回答"现在照着做什么"，分开维护

## 时间戳规范

格式：`YYYY-MM-DD HH:MM 时区缩写 (UTC±N)`，例如 `2026-07-21 14:30 CST (UTC+8)`。
- 默认精确到分钟
- 如果原始信息本身有更细的精度（比如系统日志自带秒级时间戳），如实保留，
  不刻意抹掉；反之不编造超出实际掌握程度的精度（记不清具体几分，就写
  "约 14:30"或"下午"，不要编造假精确）
- 时区必须能唯一确定：三字母缩写（CST/EST 这类）本身有歧义，必须配 UTC
  偏移量；直接写 UTC 偏移量没有歧义，可以单独用。两种都保留是为了兼顾
  人一眼认出来的熟悉感和机器/跨时区读者的准确性，不要简化成纯 ISO 8601
  ——那对人不直观

## 并发写入处理

不同文件风险不同，不用统一的锁机制：

- **CHANGELOG**：低风险，追加为主。即使两条记录时间有重叠，也如实反映了
  "这段时间确实有并行工作"，不需要协调，只需要保证写入方式是"追加"而非
  "整体重写"
- **ADR**：低风险，唯一的坑是编号冲突。解法：决定要写的当下立刻用该编号
  创建占位文件，后来者看到编号已占，自动往后排
- **STATUS**：唯一高风险点（整体改写性质）。用**软锁**：
  - 格式见上文"核心文件"一节
  - **超时视为失效**：预计时长（TTL）到了还没清除，下一个执行者可以强制
    接管，但必须在 CHANGELOG 留一笔"接管了过期的锁"
  - **有 git 的项目**：软锁是提醒，git 的 merge conflict 机制才是真正的
    保底防线
  - **没有 git 的纯文件项目**：软锁是唯一防线，如实承认它只是"依赖自觉
    遵守的约定"，不是强制机制——这个局限要写清楚，不要假装它能兜住一切
  - **任何会碰 STATUS 的 AI agent，处理任务的第一步必须是读取并检查软锁
    标记**——这条要写进对 agent 的操作指令里，不能假设它会自己想到

## 使用建议

首次启用一个项目时，创建 `STATUS.md` / `CHANGELOG.md` / `docs/adr/`
（含 `0000-template.md`）。其余毕业文件不预先创建，等触发条件满足时再建，
在触发它的核心文件位置留一行指路。

模板见 `assets/` 目录：`STATUS_TEMPLATE.md`、`CHANGELOG_TEMPLATE.md`、
`ADR_TEMPLATE.md`、`FAULT_CASES_TEMPLATE.md`、`DECISION_INDEX_TEMPLATE.md`、
`SYNC_MAP_TEMPLATE.md`、`SOP_TEMPLATE.md`。需要具体格式时直接读取对应
文件，不用背下来。
