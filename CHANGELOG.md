# Changelog — blackbox 这个 Skill 自身的创建历程

> ⚠️ **补记说明**：这份 CHANGELOG 不是实时记录的，是 2026-07-22 一次性回顾
> 整理出来的（对应 blackbox 规则 5："发现本该记录但当时没记的信息缺口，
> 发现的当下就记"——这里的"缺口"是整个创建过程从未被正式记录过）。
> 时间戳只精确到**日期级别**，因为对话过程中没有留下精确到分钟的原始
> 记录，如实标注这个精度限制，不编造假的分钟级精确度。事件顺序和内容
> 依据对话记录本身重建，可信度高；具体某句话发生在当天几点，不可考。

## [2026-07-21] 起点：为 override-rules 项目设计"完整记录"需求

- 类型：决策
- 对象：无（概念阶段）
- 原因：用户在维护 `powerfullz/override-rules` 的个人定制版时，提出一个
  独立于具体项目的需求——想要一套"从开始到结束，完整记录做了什么、怎么
  做的、哪怕停几年也能接续"的通用流程，并希望做成可复用的 Skill，而不是
  每个项目单独设计一遍
- 影响：确立了后续所有讨论的出发点

## [2026-07-21] 术语澄清与设计方向：定名"logbook"

- 类型：决策
- 原因：讨论了 GitOps / ADR / Changelog / Conventional Commits / 实验室
  笔记（Lab Notebook）等类比，明确目标不绑定 git，本地项目和线上仓库要用
  同一套结构（README/CHANGELOG/docs/adr，不做"本地版"和"线上版"两套命名）
- 决定：
  - 结构统一沿用 git 生态惯例命名（即使本地项目也用同名文件），理由是
    "以后要不要传上线，零成本"
  - 内容规则：STATUS 开工核对+收工强制更新、CHANGELOG 完成一个可独立
    描述的节点记一条、ADR 三选一门槛（影响大/有备选权衡/大概率被问为什么）
    且必须当下写不能事后补
  - 时间戳加开始/结束字段，理由是"脉络要完整，不只是知道做了什么，还要
    知道花了多久、隔了多久"
  - 触发方式：只有用户明确说"启用"才用，不因为任务是多轮/持续就主动套用；
    执行方式：日常自动写、ADR 级别决定先确认再写
  - 名字定为 `logbook`（航海日志的隐喻，从多个候选里选出）
- 关联：这是 blackbox 的前身设计

## [2026-07-21] logbook 正式打包发布

- 类型：新增
- 对象：`logbook.skill`（SKILL.md + assets/ 三份模板：README 状态章节、
  CHANGELOG、ADR）
- 验证：通过、打包成功，交付给用户

## [2026-07-21] logbook 在真实项目（override-rules-custom）中试用

- 类型：修复、决策
- 原因：把 logbook 应用到"给 override-rules 加自定义规则"这个真实项目上，
  过程中发现并修复了多处实际问题——静态资源规则集"抢跑"AI服务等专属策略
  组（范围从最初只发现 AI 服务扩大到 16 个策略组里 15 个受影响）、AI服务
  策略组设计（手动固定+故障转移兜底）、执行机制选型（GitHub Actions vs
  客户端链式覆写）
- 影响：验证了 logbook 的记录方式在真实使用中可行，同时暴露了一处文档
  内部不一致的 bug（README 标题写"开始/结束都更新"，但 SOP 实际步骤只要求
  "开始时读、结束时写"），当场修正
- 撤回：见下条——这个独立项目路线后来被整体推翻

## [2026-07-21] 重大转向：推翻独立项目方案，改用 Fork 官方源码定制

- 类型：决策、撤回
- 原因：深入澄清需求后发现，用户要的"最终产物"是一个包含上游最新代码的
  单一覆写脚本文件，不是一套独立的生成流水线。进一步查证发现上游项目
  `powerfullz/override-rules` 本身就是标准 TypeScript 项目，官方文档
  `docs/HOW_TO_CUSTOMISE.md` 明确推荐"Fork + 改 src/*.ts + 自带 CI 自动
  构建发布"这条路
- 影响：override-rules-custom 独立项目（含 custom-override.js、build.js、
  GitHub Actions 工作流）被判定为方向性错误，废弃；改为实际 Fork 上游仓库、
  直接编辑真实源码；之前的分析结论（AI服务策略组设计、静态资源问题排查）
  保留价值，迁移落地到新架构里
- 撤回：是（override-rules-custom 项目整体撤回，见新建的 ADR-0003）
- 关联：override-rules 项目自己的 `_fork/adr/0003-fork-upstream-typescript-source.md`

## [2026-07-21] 对 logbook 设计的第一次外部审视：用户提供的外部评价文本

- 类型：修复（讨论层面）
- 原因：用户提供了一份对 logbook 的批评文本（触发条件过严、ADR 门槛主观、
  ADR 缺反思机制、SOP 应默认建议、模板缺演进规则、缺工具互操作说明）
- 结论：采纳"ADR 状态机+索引"和"SOP 默认建议"两条；不采纳"客观数字替代
  主观判断"和"允许事后补 ADR"（后来在 blackbox 阶段以"补记"这种更克制的
  方式部分吸收）；触发条件严格与否判定为用户主动选择，不是设计缺陷；模板
  演进规则/工具互操作建议做成可选进阶，不进默认模板

## [2026-07-21] 发现真实的、更成熟的参照案例：custom-image-recipe

- 类型：新增（认知）
- 原因：用户分享了另一个真实项目——群晖 Docker 部署自定义 Hermes Agent
  镜像——的完整记录框架（00-README 导航、01 基础镜像溯源、03 基础设施
  快照、04 决策日志、05 升级策略、06 完整重建清单、07 故障案例库、08
  变更日志），完整阅读了全部 16 个文件（含此前遗漏的 08-变更日志.md、
  Dockerfile、proxy.conf）
- 发现：这套框架已经相当成熟，包含 logbook 当时没有的多个机制——决策
  记录的"不确定性"和"撤销/回退"字段、故障案例库、文件级时间戳备份、
  "决策变更日志"表、三方副本同步对账记录、归档文档与操作手册分离
- 影响：纠正了此前"logbook 缺原始流水层"的错误判断（案例证明流水记录
  和结构化提炼可以共存）；确立了要把这套真实经验反哺进通用框架的方向

## [2026-07-21] 决定方向：不做精简集，logbook 与这套经验合并成一套新框架

- 类型：决策
- 原因：用户明确表示不想要"两套框架、每次纠结用哪个"，要一套通用方案，
  适用于任何项目
- 决定：从这个真实案例里提炼通用设计，与 logbook 合并成新框架，不保留
  logbook 作为精简子集

## [2026-07-21] 核心设计原则确立：无极简/详细档位，固定触发规则驱动

- 类型：决策
- 原因：反复讨论"怎么触发从简到繁"，用户指出根本不需要"触发从简到繁"这个
  概念，只需要一组固定的、始终生效的记录规则，记录的详细程度是规则撞上
  现实复杂度的自然结果
- 决定：确立总体判断标准（"不知道背景但知道去哪找的人，读完能不能安全
  继续动手"）；确立"事件溯源风格的记录纪律"这个定位（借用事件溯源的几条
  原则——只追加、推翻新增不改写、任意时刻状态可还原——但不是真的做事件
  溯源系统）

## [2026-07-21] 第一轮多 AI 咨询：征集遗漏的触发规则

- 类型：新增
- 原因：已有 7 条从真实案例提炼的规则（多次尝试各记一条、备份记录、决定
  推翻新增不改写、多副本同步记录、发现盲点即记、结果不符预期即记、外部
  依赖变化即记），咨询 deepseek/gemini/gpt/minimax/grok/kimi/qwen 共 7 家
  AI，征集补充规则
- 决定：整合出 9 条新增规则（决策假设的固化/验证/证伪、排除的备选方案、
  约束条件变化、接受已知风险不处理、目标范围变化、环境依赖漂移、权限
  责任交接、记录本身矛盾需更正、不可逆操作前记录意图），合计 16 条，
  正式定稿，不再常规性新增

## [2026-07-21] 名字定为 blackbox

- 类型：决策
- 原因：logbook 正式退休，需要新名字。候选包括 labnotes/rebuild-dossier/
  forge-log/papertrail/deadreckoning/provenance/strata/codex/ledger/
  fieldnotes/chronicle
- 决定：选定 `blackbox`（飞机黑匣子隐喻——持续记录一切，事后能完整还原
  发生过什的），理由是精准对应"随时能推倒重建"这个核心诉求，且是全球通用
  意象，不需要额外解释

## [2026-07-21] 核心文件字段与毕业机制定稿（第一版）

- 类型：决策
- 内容：
  - ADR 加"不确定性""撤销/回退""接受的风险"三个永久字段
  - CHANGELOG 改为结构化字段（类型枚举/原因/影响/验证/撤回/关联等）
  - STATUS 加"关键事实区""项目状态枚举""已知盲点表"
  - 时间戳格式定为 `YYYY-MM-DD HH:MM 时区缩写 (UTC±N)`，默认精确到分钟，
    如实记录已知精度、不编造假精度；期间用户追问"CST 是不是中国标准时间"，
    澄清 CST 本身有歧义（中国/美国中部/古巴标准时间都用这个缩写），确立
    "缩写必须配 UTC 偏移量"这条子规则
  - 毕业机制分两种触发路径：数量触发型（故障案例库，阈值 3）、存在触发型
    （决策变更表、多副本同步地图≥3、操作手册），基础设施快照判定为不独立
    成文件类型，拆进"关键事实"+CHANGELOG历史

## [2026-07-21] 并发处理方案确立：STATUS 软锁

- 类型：决策
- 原因：讨论并发写入风险（CHANGELOG/ADR 低风险，STATUS 整体改写性质、
  高风险）。用户提出"改成简单数据库"的设想，评估后判定代价过高（牺牲
  "任何人打开就能读懂"、丢失 git 天然审计能力、破坏"任何环境都能用"），
  且数据库解决的是"实时协调问题"，与 blackbox 要解决的"历史记录问题"
  性质不同，不建议采用
- 决定：STATUS 用软锁（结构化标记：谁/从/打算做什么），有 git 项目软锁
  是提醒、git merge conflict 是保底；无 git 项目软锁是唯一防线。用户确认
  "现在用软锁，以后多 agent 场景可能升级成真锁"，评估了软锁→真锁的升级
  成本（信息字段不用变，需要新增的是原子性保证和超时失效机制）

## [2026-07-21] 第二轮多 AI 咨询：审核已定型设计的漏洞

- 类型：修复
- 原因：设计接近定稿，咨询 deepseek/gemini/minimax/gpt/grok/qwen 共 6 家
  AI，这次改为"挑毛病"而非"补规则"，聚焦五个问题：内在矛盾、长期会被
  放弃执行的过度设计、毕业阈值合理性、软锁失效场景、其他结构性盲区
- 决定（强共识，直接采纳）：
  - ADR"不能事后补"与规则 5 冲突 → 加"补记"例外通道，显式标注、不算洗白
  - 规则 1（多次尝试记录）无颗粒度定义 → 定义为"意图/策略变化或产生
    可观察不同结果"的单位，同一意图下连续微调可合并
  - 多副本同步地图阈值 ≥3 太晚 → 改为 ≥2（且限定"仍在读写使用、无自动
    强同步机制"的副本）
  - 故障案例库仅按次数（3次）会让严重的单次事故被拖延立档 → 改为双触发
    （3次同类 或 单次影响足够严重）
  - STATUS 软锁缺超时机制 → 加 TTL（预计时长），超时视为失效可强制接管
  - AI agent 不会主动读锁标记 → 写进操作规范，不是文件格式能解决的问题
  - 很多"规则冲突"其实是同一事件天然命中多条规则 → 补一条元规则：允许
    一条记录同时标注命中的多类，不强制拆分
- 决定（部分采纳，个别观点）：
  - CHANGELOG 字段分两层（基础必填 + 决策/修复/重建/撤回/不可逆类型才
    强制扩展字段），避免全字段强制导致长期被弃用
  - 规则 3（决定被推翻）与规则 15（记录本身有误）语义边界明确区分
  - 验证结果字段允许"未验证"作为合法值，不止"通过/失败"二元
  - CHANGELOG 需要归档/裁剪机制（按年/里程碑归档旧记录）
  - CHANGELOG 加 author（human/ai）与 verified_by 建议字段
- 决定（不采纳）：
  - gemini 建议时间戳改用纯 ISO 8601 格式 —— 不采纳，因为"缩写+UTC偏移"
    格式是深思熟虑过的取舍（兼顾人类可读性），纯 ISO 8601 对人不直观，
    与 blackbox"人也要能直接读懂"的目标冲突

## [2026-07-21] 两处收尾细节确认

- 类型：决策
- 内容：
  1. CHANGELOG 类型字段允许多选（一条记录同时标注命中的多个类型），只要
     命中类型中有一个在强制扩展字段清单里，就按强制字段执行
  2. 规则3（决定推翻）与规则15（记录有误）都命中同一事件时，算一条记录、
     同时标注两个类型，不强行拆成两条

## [2026-07-21～2026-07-22] blackbox SKILL.md 正式定稿并打包

- 类型：新增
- 对象：`blackbox.skill`（SKILL.md + assets/ 七份模板：STATUS、CHANGELOG、
  ADR、故障案例库、决策变更索引、多副本同步地图、操作手册）
- 影响：logbook 正式退休；此前给 override-rules 项目做的
  `override-rules-fork-ops.skill`（项目专属操作手册）不受影响，继续独立
  使用，与 blackbox 是否合并是后续可选的事，本次未处理
- 验证：通过 skill-creator 校验，打包成功，交付给用户

## [2026-07-22] 补记本篇 CHANGELOG 本身

- 类型：新增
- 原因：用户要求整理 blackbox 创建过程的完整记录，供其保存
- 说明：本文件从对话记录回顾重建，非实时记录，时间戳精度限定在日期级别，
  已在文件开头显式声明"补记"性质

---

## [2026-07-22] 真实使用中发现并修复：记录被当成收尾动作，而非操作本身的一部分

- 类型：修复
- 对象：`blackbox` SKILL.md
- 原因：用户已把 blackbox 交给 hermes 用于维护 override-rules 项目（见
  ADR-0001 提到的"未迁移"事项——实际是继续沿用 logbook 时期的记录，
  这次是 hermes 在实际执行任务时首次真正触发 blackbox 规则）。hermes
  修改代码、验证结果后，没有同步更新 CHANGELOG，直到用户主动问"记录了
  吧？"才回头补记。hermes 自我归因：把"完成操作"和"记录操作"当成了
  两个可以先后错开的独立任务
- 影响：暴露了 blackbox 设计上的真实空白——规范只定义了"什么事件触发
  必须记录"，没有明确"记录这个动作本身该立即写入文件，还是可以攒到
  操作全部完成后再写"。这个空白如果不补，会让 blackbox 抗会话中断这个
  核心目的失效（中途中断的话，攒着没写的部分就跟没发生过一样）
- 修复：在 SKILL.md 里新增"记录的写入时机"一节，明确 write-through
  原则
- 验证：无自动化验证（文字规范层面的修改），通过两轮外部 AI 咨询审核
  修复方案本身
- 关联：ADR-0002

## [2026-07-22] 衍生问题的修复：AI agent 不能擅自修改共享规范定义

- 类型：修复
- 对象：`blackbox` SKILL.md
- 原因：hermes 发现上述问题后，未经确认直接修改了它本地那份 blackbox
  skill 文件本身。blackbox 是跨项目共享的定义，不是单个项目专属文件，
  agent 单方面修改会导致版本分叉，且这类"影响面大、以后会被问为什么"
  的改动，按 blackbox 自己的精神本该走确认流程，却被跳过了
- 影响：新增"修改这份 SKILL.md 本身，需要走确认流程"一节；要求疑似
  漏洞走结构化上报（场景/影响/建议修改，标"待确认"），不能直接改共享
  定义，也不能混进 CHANGELOG（那是给已确认变更用的）
- 验证：无自动化验证，通过第二轮外部 AI 咨询审核（六家：ChatGPT/
  Gemini/DeepSeek/Grok/Kimi/Qwen，聚焦评估这一具体修复方案，非泛泛
  审核整体设计）
- 关联：ADR-0002

## [2026-07-22] 第二轮修复的再优化：写入时机规则升级

- 类型：修复
- 对象：`blackbox` SKILL.md
- 原因：初版"立即写入"规则被指出可能导致过度碎片化（每个微小改动都
  单独记一条，产生噪音）；且"立即写入"作为一条正向要求，约束力不如
  "未记录即未完成"这种反向不变量表述
- 修复：
  1. 记录颗粒度直接复用规则 1 已有的"原子单位"定义，不新造概念，同时
     解决"太碎"和"太粗"两个方向的问题
  2. 核心表述改为"记录没有落盘之前，这件事就不算完成"（不变量式），
     取代单纯的"必须立即写"（义务式）
  3. 新增"骨架先行"机制：跨越多步骤或有风险的原子操作，开始前先写
     骨架记录（时间戳+打算做什么+状态:进行中），完成后原地补全，而非
     新开一条——直接对应"中断后已发生部分不会消失"这个核心目的
  4. 补上写入失败时的处理（不能忽略，必须停下或至少标记失败本身）
  5. 共享规范上报改为结构化条目，不再是"随手记一句"
- 验证：无自动化验证，方案经六家 AI 咨询审核后综合
- 关联：ADR-0002

## [2026-07-24] 把 blackbox 本体纳入 git 版本控制

- 开始：2026-07-24
- 结束：2026-07-24
- 类型：新增 / 决策
- 对象：新建独立 git 仓库，包含 SKILL.md、assets/、STATUS.md、
  CHANGELOG.md（本文件）、adr/、README.md
- 原因：排查 override-rules-fork-ops 真实违规事故时，暴露出 blackbox
  没有独立可核验的存放位置这个根本问题——之前只能靠对话交付和口头/
  记录层面的"信任"来确认版本一致性，无法真正核实
- 影响：以后任何人（含 hermes）想确认某份 blackbox 副本是不是官方
  最新版，直接对照这个仓库即可
- 验证：不适用（架构决定，非代码变更）
- 撤回：否
- 关联：ADR-0003

---

## [2026-08-01] 真实使用中发现并修复：会话交接产生的卫星文档，无法被新会话发现

- 开始：2026-08-01（精确时刻未记录，按 blackbox 自身惯例如实标注精度
  限制，不编造假的分钟级精确度）
- 结束：2026-08-01
- 类型：修复、新增
- 对象：`blackbox` SKILL.md、`assets/STATUS_TEMPLATE.md`
- 原因：下游真实使用项目 `hermes-nas-experiment` 在 2026-07-31 限流期间
  发生 AI 会话交接（Claude chat 限额恢复期间由 Claude Code 临时接管
  协调），交接产生两份独立文档（限流期接续工作总纲.md、待Claude-chat
  拍板清单.md），完整记录了交接事实/未完成事项，符合规则14要求。但这
  两份文档独立于四件套之外，STATUS.md 未建立任何索引。2026-08-01 一个
  全新对话窗口按标准流程读完 STATUS→CHANGELOG，完全不知道这两份文档
  存在，直到该项目的第三方审核者（opencode）在执行另一项无关审计任务
  时偶然扫到文件名，问题才浮出水面
- 影响：暴露规则14的真实空白——只要求"交接要记录"，没要求"记录必须
  可被发现"。"记录详细"（完整性）和"记录能被找到"（发现性）是两件
  不同的事，这次事故证明只做到前者不够。新增"毕业文件"类别"交接文档
  索引"，要求任何产生独立卫星文档的会话交接，必须同步在 STATUS.md
  建立索引小节；同步更新 STATUS_TEMPLATE.md 加入占位示例
- 验证：无自动化验证（文字规范层面修改），逻辑上对照本次真实事故的
  失效模式做了针对性修复，尚未在更多真实项目中检验
- 是否后续被撤回：否
- 关联：ADR-0004

---

## [2026-08-01] 补记：blackbox-project-record 文件夹正式归档

- 开始：2026-08-01（补记，精确时刻未记录）
- 结束：2026-08-01
- 类型：归档、新增
- 对象：`blackbox-project-record`（本仓库出现前，blackbox 自身历史的
  临时存放文件夹，仅含 STATUS.md/CHANGELOG.md/adr/，不含 SKILL.md 源
  文件本身）
- 原因：2026-07-24 blackbox 本体纳入本 git 仓库时（见上条、ADR-0003），
  `blackbox-project-record` 文件夹的 STATUS.md 已在**它自己那份文件里**
  记录了"已归档"状态，但本仓库（唯一权威版本）当时没有对应留一笔——
  这导致"旧记录被正式退役"这个事实，只活在一份此后大概率不会再被
  打开的旧文件里，本仓库读者无法得知它曾经存在过、现状如何
- 影响：本条作为补记，把这个事实带回权威版本这一侧。`blackbox-project-
  record` 文件夹自 2026-07-24 起正式归档，不再独立维护，其历史内容
  （STATUS/CHANGELOG/adr/0001/adr/0002）已核对与本仓库一致（2026-08-01
  逐字比对确认 adr/0001、adr/0002 无差异），不再需要迁移或合并任何
  额外内容
- 验证：通过（逐字节 diff 比对 `blackbox-project-record` 与本仓库
  2026-07-24 版本的 STATUS.md/CHANGELOG.md/adr/0001/adr/0002，确认除
  "归档声明"这一条差异外，其余内容完全一致）
- 是否后续被撤回：否
- 关联：ADR-0003
- author: ai（用户提供 blackbox-project-record.zip，要求比对本仓库并
  补齐缺失内容）
- verified_by: 空
